// Componente legado — substituído pela página /otica
// Mantido apenas para referência; não é renderizado no app.
export default function OticaPanel(_props: { onClose: () => void }) {
  return null
}
